# FOC_ST_V2 TODO (2026-06-11 → 2026-07-08)

> 基于当前代码全面扫描。状态: 🔴 必须修 / 🟡 应该修 / 🟢 锦上添花 / ✅ 已修复
> **上次更新**: 2026-07-08 — 大量 P0/P1 项已修复，文档同步至最新进度

---

## 🔴 P0 — 逻辑错误，影响运行

> ✅ **P0 全部清零！** 所有致命逻辑错误已于 6.11~7.08 期间修复。

---

## 🟡 P1 — 功能缺失

### 1. `Balance_Control()` 为空

- **文件**: `taskManager.c:42-45`
- 如果不需要这个任务，建议从 `TasksPare[]` 中移除，避免空转。

### 2. Mode 1 (有感 FOC) 的角度来源

- 当前 Mode 1 代码链路已通 (`UVW_Axis_DQ` + `Speed_FOC` + `Idq_FOC` + `FOC_Svpwm_dq`)，仅缺传感器角度来源
- Mode 1 应接入外部传感器 (Hall/Encoder) → 直接给 `motor.IQAngle`

---

## 🟢 P2 — 代码质量

### 3. 任务调度器改进

- `Task_Manage_List_Init()` 中 Task_Count 硬编码初始值（非零），建议统一从 0 开始
- 任务数量 `Task_Num=5` 为宏 → 建议用 `sizeof(TasksPare)/sizeof(TasksPare[0])` 自动计算

### 4. `key_driver.c` 多处重复代码

- `key_array_init()` 中 4 个 key 的初始化完全相同，可用 `for` 循环替代
- `key_check()` 中 4 个 key 逐个调用 `hold_filter`，也是复制粘贴

### 5. MOTOR_LS 实测验证

- 当前 `MOTOR_LS = 20µH` 高度可疑（52 极电机通常电感更大）
- 建议用 LCR 表 1kHz 实测：任意两相电感 ÷ 2

---

## ⚪ P3 — 远期优化

### 6. 电机参数可切换

- 当前 `MOTOR_RS/LS/FLUX/POLES` 为 `#define`，换电机需重新编译
- 方案: 参数表 + `Motor_Switch(id)` API

### 7. FDCAN 通信协议

- `fdcan.c` 已初始化但未定义应用层协议
- 可用于多机协同或上位机控制

### 8. Flash 参数存储

- PI 参数、电机参数存入 Flash，上电自动加载

---

## ✅ 已修复 (2026-06-11 修复)

| # | 项目 | 修改位置 |
|---|------|----------|
| 1 | VF 模式多余 Angel_Get() 删除 | `motorControl.c:57-61` |
| 2 | LCD 模式标签对齐 (STOP/SENSOR/SENSORLESS/VF/PREPOS) | `taskManager.c` 两处 |
| 3 | Ctrl_ts 修正: 0.0005→5e-5 (20kHz) | `flux.c:29` |
| 4 | 速度系数修正: 650→1/(8×Ts×2π) 自动计算 | `flux.c:122` |
| 5 | InvProtect 实现 (过流/过压/欠压 + 锁存 + 清除) | `InvProtect.h/c` + `motorControl.c:42` |
| 6 | PI 积分分离: Out==v1 浮点比较 → 限幅检测 | `PI_Cale.c:7-9` |
| 7 | 按键功能映射 (启动/停止/模式切换/频率±) | `taskManager.c:KEY_RUN` + `key_driver.h` |
| 8 | SVPWM 母线电压补偿启用 + Foc_Para_Calc 计算 Km | `svpwm.c` + `ADC_Sample.c` |
| 9 | Mode 4 预定位实现 (VF+观测器预热, 不自动切) | `motorControl.c:74-99` |
| 10 | key_bsp.h 引脚冲突修复 (key_5 重复 PC9) | `key_bsp.h:19-25` |

---

## ✅ 已修复 (2026-06-25 ~ 07-08 修复)

| 项目 | 说明 |
|------|------|
| Observer_Run() 恢复 | 去掉 IQmath，用 sinf/cosf，死区补偿 + PLL 抗饱和 + 同周期 Err |
| Idq_FOC() 恢复 | motor.V_q 正确赋值，电压圆约束动态限幅 |
| FOC_Svpwm_dq() 恢复 | iPark → SVPWM → PWM_Update_From_SVPWM |
| Angel_Get() 角度回绕 4095→4096 | 已修复 |
| Mode 2 完整 FOC 链路 | Angel_Get + UVW_Axis_DQ + Speed + Idq + SVPWM |
| 变量命名 Flux/flux 统一 | 统一为大写 F |
| PI2 宏缺失 | 改用 2.0f * PI |
| I_d_GXieLv 声明 | 已取消注释 |
| IPARK_PVdq 声明 | 已取消注释 |
| PLL 积分抗饱和 | Limit_Sat ±0.157 rad/周期 |
| Gain 15000→5000 | 降低磁链补偿震荡 |
| VF 分离架构 | Mode 4 只预热不自动切换 + Mode 2 暖启动检测 |
| VF 全部 Bug 修复 | 除零保护 + TargetVolt 不清零 + PWM 输出 + while 角度归一 + static |
| 电流采样负号 | 三相电流 + 母线电流加回负号 |
| PI 限幅修正 | 速度环 12A→3A, 电流环 12pu→1pu |
| Foc.h 注释 | 乱码清理 + 冗余字段移除 + Control_Mode 注释修正 |
| Hybrid Active Flux 观测器 | 二阶系统带宽设计 PLL + 反馈补偿 PI + SMO 备用 |
| Vofa 通道扩展 | 6ch→9ch，新增三相电流原始值 |

---

## ⚠️ 仍需实际硬件调参

| 项目 | 优先级 |
|------|--------|
| MOTOR_LS 用 LCR 表实测 | 🟡 |
| 不同负载下 PI 参数精调 | 🟢 |
| 死区补偿电压 V_DEADTIME 实测标定 | 🟢 |
| Hybrid 观测器 vs 电压型观测器对比测试 | 🟢 |

| 参数 | 当前值 | 说明 |
|------|--------|------|
| pi_spd Kp/Ki | 0.1 / 0.01 | 速度环, 需根据实际惯量调 |
| pi_id Kp/Ki | 1.0 / 0.1 | d轴电流环 |
| pi_iq Kp/Ki | 1.0 / 0.1 | q轴电流环 |
| PLL kp/ki | 20 / 10 | PLL 锁相环 |
| Observer Gain | 150000 | 磁链幅值补偿增益 |
| I_PREPOS_A | 2.0A | 预定位电流 |
| NOMINAL_BUS | 24V | 标称母线电压 |

> PI 参数初始值是保守估算值，必须根据实际电机运转情况调整。建议通过 Vofa 上位机观察 Iq/Iq_ref 跟随情况，先调电流环再调速
